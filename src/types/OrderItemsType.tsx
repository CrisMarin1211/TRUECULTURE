export interface EventActivity {
  eventLetter: string;
  eventName: string;
  participants: number;
  percent: number;
}

export interface EventSales {
  itemName: string;
  totalQuantity: number;
  totalPrice: number;
}